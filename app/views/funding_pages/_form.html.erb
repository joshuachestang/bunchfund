<%= form_for(@funding_page) do |f| %>
  <% if @funding_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funding_page.errors.count, "error") %> prohibited this funding_page from being saved:</h2>

      <ul>
      <% @funding_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
